package app.backend.models;

// emum = enables variable to be a set of predefined constants
// different roles a user can have in this app
public enum PredefinedRoles {
  ROLE_USER,
  ROLE_MODERATOR,
  ROLE_ADMIN,
}
